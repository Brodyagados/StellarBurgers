import { TBaseReducerAction } from '.';
import { IngredientModel } from '../../models';

const initialState: IngredientModel[] = [];

export const ingredientsReducer = (state = initialState, action: TBaseReducerAction) => {};
